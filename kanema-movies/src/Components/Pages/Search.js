import React from "react";
import MovieSearch from "../MovieSearch/MovieSearch";

const Search = () => {
    return (
        <div>
            <MovieSearch />
        </div>
    );
};
export default Search;